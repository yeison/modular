--extra-index-url https://download.pytorch.org/whl/cpu

-r ../requirements.in

torch==2.7.0; platform_machine != "x86_64"
torchaudio; platform_machine != "x86_64"
torchvision; platform_machine != "x86_64"

torch==2.7.0+cpu; platform_machine == "x86_64"
torchaudio; platform_machine == "x86_64"
torchvision; platform_machine == "x86_64"
